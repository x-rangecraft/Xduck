[Service]
# A disconnected USB CDC read may not return on SIGTERM. Bound service replacement so updaterd's
# 30-second health gate measures the new release rather than waiting for the old process to exit.
TimeoutStopSec=5s
